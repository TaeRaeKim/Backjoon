#include <bits/stdc++.h>
using namespace std;
using pos = pair<int, int>;

int N;
int arr[125][125];
int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);

	while (true) {
		cin >> N;
		if (N == 0) break;
		queue<pos> q;
		for (int i = 0; i < N; i++) for (int j = 0; j < N; j++) cin >> arr[i][j];



	};


	return 0;
}